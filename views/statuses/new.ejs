<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Добавить статус брони</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h1>Добавить статус брони</h1>
    <form action="/statuses" method="POST">
        <div class="mb-3 form-check">
            <input type="checkbox" name="is_booked" class="form-check-input" id="is_booked">
            <label class="form-check-label" for="is_booked">Забронировано</label>
        </div>
        <div class="mb-3 form-check">
            <input type="checkbox" name="is_paid" class="form-check-input" id="is_paid">
            <label class="form-check-label" for="is_paid">Оплачено</label>
        </div>
        <div class="mb-3 form-check">
            <input type="checkbox" name="is_cleaning" class="form-check-input" id="is_cleaning">
            <label class="form-check-label" for="is_cleaning">На уборке</label>
        </div>
        <button type="submit" class="btn btn-primary">Добавить</button>
        <a href="/statuses" class="btn btn-secondary">Назад</a>
    </form>
</div>
</body>
</html> 