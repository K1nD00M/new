<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Категории услуг</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h1>Категории услуг</h1>
    <a href="/" class="btn btn-secondary mb-3">На главную</a>
    <a href="/service_categories/new" class="btn btn-success mb-3">Добавить</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Название</th>
                <th>Описание</th>
            </tr>
        </thead>
        <tbody>
            <% serviceCategories.forEach(category => { %>
                <tr>
                    <td><%= category.id_категория %></td>
                    <td><%= category.название %></td>
                    <td><%= category.описание %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
</body>
</html> 