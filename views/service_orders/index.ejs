<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Заказы услуг</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h1>Заказы услуг</h1>
    <a href="/" class="btn btn-secondary mb-3">На главную</a>
    <a href="/service_orders/new" class="btn btn-success mb-3">Добавить</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Бронь</th>
                <th>Услуга</th>
                <th>Количество</th>
                <th>Дата заказа</th>
                <th>Сумма</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            <% serviceOrders.forEach(order => { %>
                <tr>
                    <td><%= order.id %></td>
                    <td><%= order.booking_id %></td>
                    <td><%= order.service_id %></td>
                    <td><%= order.quantity %></td>
                    <td><%= order.order_date %></td>
                    <td><%= order.total_price %></td>
                    <td>
                        <a href="/service_orders/<%= order.id %>/edit" class="btn btn-sm btn-primary">Редактировать</a>
                        <form action="/service_orders/<%= order.id %>?_method=DELETE" method="POST" style="display: inline;">
                            <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Вы уверены?')">Удалить</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
</body>
</html> 