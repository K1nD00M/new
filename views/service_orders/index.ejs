<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Заказы услуг</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h1>Заказы услуг</h1>
    <a href="/" class="btn btn-secondary mb-3">На главную</a>
    <a href="/service_orders/new" class="btn btn-success mb-3">Добавить</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Бронь</th>
                <th>Услуга</th>
                <th>Дата заказа</th>
                <th>Сумма</th>
                <th>Статус</th>
            </tr>
        </thead>
        <tbody>
            <% serviceOrders.forEach(order => { %>
                <tr>
                    <td><%= order.id_заказ %></td>
                    <td><%= order.id_бронь %></td>
                    <td><%= order.id_услуга %></td>
                    <td><%= order.дата_заказа %></td>
                    <td><%= order.сумма %></td>
                    <td><%= order.статус %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
</body>
</html> 