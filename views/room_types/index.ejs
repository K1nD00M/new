<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Типы номеров</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h1>Типы номеров</h1>
    <a href="/" class="btn btn-secondary mb-3">На главную</a>
    <a href="/room_types/new" class="btn btn-success mb-3">Добавить</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Название типа</th>
                <th>Описание</th>
                <th>Базовая цена</th>
            </tr>
        </thead>
        <tbody>
            <% roomTypes.forEach(type => { %>
                <tr>
                    <td><%= type.id_тип_номера %></td>
                    <td><%= type.название_типа %></td>
                    <td><%= type.описание %></td>
                    <td><%= type.базовая_цена %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
</body>
</html> 