<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Типы номеров</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h1>Типы номеров</h1>
    <a href="/" class="btn btn-secondary mb-3">На главную</a>
    <a href="/room_types/new" class="btn btn-success mb-3">Добавить</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Название типа</th>
                <th>Описание</th>
                <th>Базовая цена</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            <% roomTypes.forEach(type => { %>
                <tr>
                    <td><%= type.id %></td>
                    <td><%= type.name %></td>
                    <td><%= type.description %></td>
                    <td><%= type.base_price %></td>
                    <td>
                        <a href="/room_types/<%= type.id %>/edit" class="btn btn-sm btn-primary">Редактировать</a>
                        <form action="/room_types/<%= type.id %>?_method=DELETE" method="POST" style="display: inline;">
                            <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Вы уверены?')">Удалить</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
</body>
</html> 