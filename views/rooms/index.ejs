<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Номера</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h1>Номера</h1>
    <a href="/" class="btn btn-secondary mb-3">На главную</a>
    <a href="/rooms/new" class="btn btn-success mb-3">Добавить</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Номер комнаты</th>
                <th>Тип номера</th>
                <th>Цена за ночь</th>
                <th>Вместимость</th>
                <th>Статус</th>
                <th>Отзыв</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            <% rooms.forEach(room => { %>
                <tr>
                    <td><%= room.id %></td>
                    <td><%= room.room_number %></td>
                    <td><%= room.room_type_id %></td>
                    <td><%= room.price_per_night %></td>
                    <td><%= room.capacity %></td>
                    <td><%= room.status %></td>
                    <td><%= room.review %></td>
                    <td>
                        <a href="/rooms/<%= room.id %>/edit" class="btn btn-sm btn-primary">Редактировать</a>
                        <form action="/rooms/<%= room.id %>?_method=DELETE" method="POST" style="display: inline;">
                            <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Вы уверены?')">Удалить</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
</body>
</html> 